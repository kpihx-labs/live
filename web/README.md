# live-web

Immersive single-page client (Bun + Vite + TypeScript).

```bash
bun install
bun run dev      # http://127.0.0.1:5173 — proxies /api and /ws to brain
bun run build    # dist/ for production nginx image
```
