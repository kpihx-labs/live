{
  "name": "live-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "live immersive client — Bun + Vite + TypeScript",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.8.0",
    "vite": "^6.2.0"
  }
}
